{% extends 'base.html.twig' %}

{% block title %}Armour{% endblock %}

{% block body %}
    <h1>Armour</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ armour.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ armour.name }}</td>
            </tr>
            <tr>
                <th>Durability</th>
                <td>{{ armour.durability }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('armour_index') }}">back to list</a>

    <a href="{{ path('armour_edit', {'id': armour.id}) }}">edit</a>

    {{ include('armour/_delete_form.html.twig') }}
{% endblock %}
