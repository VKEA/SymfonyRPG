{% extends 'base.html.twig' %}

{% block title %}ItemInventory{% endblock %}

{% block body %}
    <h1>ItemInventory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_inventory.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('item_inventory_index') }}">back to list</a>

    <a href="{{ path('item_inventory_edit', {'id': item_inventory.id}) }}">edit</a>

    {{ include('item_inventory/_delete_form.html.twig') }}
{% endblock %}
