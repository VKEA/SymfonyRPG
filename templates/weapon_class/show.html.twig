{% extends 'base.html.twig' %}

{% block title %}WeaponClass{% endblock %}

{% block body %}
    <h1>WeaponClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weapon_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ weapon_class.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('weapon_class_index') }}">back to list</a>

    <a href="{{ path('weapon_class_edit', {'id': weapon_class.id}) }}">edit</a>

    {{ include('weapon_class/_delete_form.html.twig') }}
{% endblock %}
