<form method="post" action="{{ path('weapon_class_delete', {'id': weapon_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weapon_class.id) }}">
    <button class="btn">Delete</button>
</form>
