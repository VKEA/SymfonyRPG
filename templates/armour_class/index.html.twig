{% extends 'base.html.twig' %}

{% block title %}ArmourClass index{% endblock %}

{% block body %}
    <h1>ArmourClass index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for armour_class in armour_classes %}
            <tr>
                <td>{{ armour_class.id }}</td>
                <td>{{ armour_class.name }}</td>
                <td>
                    <a href="{{ path('armour_class_show', {'id': armour_class.id}) }}">show</a>
                    <a href="{{ path('armour_class_edit', {'id': armour_class.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('armour_class_new') }}">Create new</a>
{% endblock %}
