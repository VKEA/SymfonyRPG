{% extends 'base.html.twig' %}

{% block title %}ArmourClass{% endblock %}

{% block body %}
    <h1>ArmourClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ armour_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ armour_class.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('armour_class_index') }}">back to list</a>

    <a href="{{ path('armour_class_edit', {'id': armour_class.id}) }}">edit</a>

    {{ include('armour_class/_delete_form.html.twig') }}
{% endblock %}
